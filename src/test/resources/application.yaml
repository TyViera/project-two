spring:
  application:
    name: project-two
  datasource:
    driver-class-name: "org.h2.Driver"
    url: "jdbc:h2:mem:test"
    username: "root"
    password: ""
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: create
        show_sql: true
